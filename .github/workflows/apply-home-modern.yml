name: Apply YOCEWOR Modern Homepage
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy homepage injection is disabled; the clean Sarkari Result-style homepage is maintained directly in index.html."
